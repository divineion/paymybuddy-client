import AuthForm from "@/components/Forms/AuthForm";

const RegisterPage = () => {
    return (
        <main className={"main"}>
            <AuthForm mode={"register"}/>
        </main>
    )
}

export default RegisterPage;